## 2017/04/20
> **今天學到的事情，沒必要幫使用者做到所有考量**
> **那只會把自己綁死，要能分出哪些是絕對必要的**
> 記得也聽過，一個好的程式語言不應該禁止程序元拿石頭砸自己的腳

一個arduino不知道是只能容許一個頻道還是begin只取最後一個有效
> 原來是兩個以上要加入 BT_Uart.listen();
> 此外不應該在每次執行都呼叫 listen() 如果這是好的做法
> 作者沒必要特別把他們分出來，直接包進print就好了

新的藍芽LED特別亮，應該是沒加電阻，除了刺眼之外電力還會不足QQ
> 只能額外供電或是指令下完拔下來，不然連結時會失敗，貌似需要大電力

## 為什麼AT等待模式做不好
跟 有沒有先送一次AT指令有關??
> 後來莫名好了不曉得怎樣


## 2017/04/18
wile與if是一樣意思的，只不過wile會搶奪CPU資源

## 2017/04/17
實在想不多怎麼在單一個物件內操作，也做不出類別函式轉發
把自己當作客戶來實作，沒想到簡單多了，歸功於函式庫的撰寫考量的足夠

把使用者介面在封裝一次！或許就可行了，還可以同時保留ONECE功能並把
特化的函式隱藏起來，透過FRIEND傳過去

## 2017/04/16
class funcion point 的使用方法有點特別

```cpp
class A{
public:
    void fun(){
        cout << "A::fun()" << endl;
    }
};
int main(int argc, char const *argv[]){
    A a;
    a.fun();

    using fp = void(A::*)(void);
    fp f= A::fun;

    (a.*f)();
}
```

改了之後發現一件事情，函式成員的使用會交錯，發現可以直接在前面寫

```cpp
class BT_ATC;
```

這樣就能找到了

一切都完善了之後發現還有一個大問題，呼叫函式成員還要有一個物件，沒有那物件空有指標也沒用
因此我的委託function執行出了問題，他沒有物件
參數加入物件來呼叫，不過不知道為什麼還是不行，在C++上是可以跑的說


## 2017/04/15
### WTF 為什麼從第一次送含關鍵字的命令會跑出一個0
而且如果沒有加

```cpp
else {
    cmd_flag=1;
}
```

就會導致第一次的命令沒有被捕捉到

> 特麼78 因為OK有兩個字ㄚㄚ
> 第一掃到O就先把O發了當然會帶O
> 
> 除非足夠簡單否則不要亂簡寫 if 裡面的 !=0 會弄亂自己
> 非必要也不要 !=0 等於1 或等於0 絕對優於這個寫法，不要把自己弄亂了

> 結果只是判定方法沒寫好...一次讀完再判定比較簡單
> 非效能上的必要還是簡單為好

### 為什麼收到的字串執行命令的程序會連續執行
執行好幾次QQ

> 乾 原來那個判斷式子是在偵測字元裡面，當然幾個字元就執行幾次

## 2017/04/12
快捷鍵問題，直接發問作者秒解決

拆檔有問題不知道怎麼拆
> 原來她會自動引入那個目錄的所有檔案，直接拆檔用.h連結就好了







